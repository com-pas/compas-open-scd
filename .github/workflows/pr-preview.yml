name: Deploy PR previews

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

concurrency: preview-${{ github.ref }}
env:
  PR_NUMBER: ${{ github.event.number }}

jobs:
  deploy-preview:
    permissions: write-all
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          submodules: "true"

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Install dependencies
        if: github.event.action != 'closed'
        run: |
          npm i @nx/nx-linux-x64-gnu
          npm clean-install

      - name: Build compas-open-scd
        if: github.event.action != 'closed'
        run: |
          npm run build

      - name: Rewrite /public paths for PR preview
        if: github.event.action != 'closed'
        run: |
          sed -i "s|/public|/compas-open-scd/pr-preview/pr-${PR_NUMBER}/public|g" packages/compas-open-scd/build/index.html

      - name: Deploy preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: packages/compas-open-scd/build
          umbrella-dir: pr-preview
